/// @description

scr_setTilemapFromCurrentMap("Environment");

//var _layerId = layer_get_id("Environment");
//envTilemapId = layer_tilemap_get_id(_layerId);



/*
numCellsWidth = tilemap_get_width(envTilemapId);
numCellsHeight = tilemap_get_height(envTilemapId);

#region Map setup

// add the border of no-go tiles
for(var _x=0 ; _x<numCellsWidth ; _x++)
{
	tilemap_set(envTilemapId, eTileMapCells.envBorder, _x, 0);
	tilemap_set(envTilemapId, eTileMapCells.envBorder, _x, numCellsHeight-1);
}
for(var _y=0 ; _y<numCellsHeight ; _y++)
{
	tilemap_set(envTilemapId, eTileMapCells.envBorder, 0, _y);
	tilemap_set(envTilemapId, eTileMapCells.envBorder, 0, numCellsWidth-1);
}
#endregion
*/
